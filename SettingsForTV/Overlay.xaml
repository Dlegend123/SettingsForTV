<Window x:Class="SettingsForTV.Overlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        mc:Ignorable="d"
        Title="Overlay" MaxHeight="76" MaxWidth="74"
        TextOptions.TextFormattingMode="Display" WindowStyle="None" MouseDown="Window_MouseDown"
        Background="Transparent"
        AllowsTransparency="True" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">
    <Grid UseLayoutRounding="True">
        <Border CornerRadius="20" Background="Black" BorderThickness="1" HorizontalAlignment="Center"
                VerticalAlignment="Center" BorderBrush="White">
            <Grid>
                <Border Name="Mask"
                        Background="Black"
                        BorderBrush="Black"
                        BorderThickness="1"
                        CornerRadius="20" />
                <StackPanel UseLayoutRounding="True">
                    <Image HorizontalAlignment="Center" VerticalAlignment="Bottom" gif:ImageBehavior.AutoStart="True"
                           gif:ImageBehavior.AnimatedSource="/artifact.gif"
                           Stretch="UniformToFill" RenderOptions.EdgeMode="Aliased">

                        <Image.ContextMenu>
                            <ContextMenu>
                                <MenuItem />
                                <MenuItem />
                            </ContextMenu>
                        </Image.ContextMenu>
                    </Image>
                    <StackPanel.OpacityMask>
                        <VisualBrush Visual="{Binding ElementName=Mask}" />
                    </StackPanel.OpacityMask>
                </StackPanel>

            </Grid>

        </Border>

    </Grid>


</Window>